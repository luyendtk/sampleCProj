/*
 * randomNums.c
 *
 *  Created on: May 11, 2022
 *      Author: vbe_luyendangtk
 */

#include <stdio.h>
#include <stdlib.h>
/*
int main () {
	int numRan, numMin, numMax, numCnt;
	printf("Enter a minimum value: \t");
	scanf("%d", &numMin);
	printf("Enter a maximum value: \t");
	scanf("%d", &numMax);
	printf("Enter num of random numbers in range from %d to %d: \t", numMin, numMax);
	scanf("%d", &numCnt);
	int randomNums[numCnt];
	for (int i = 0; i < numCnt; i++) {
		numRan = rand() % numMax + numMin;
		randomNums[i] = numRan;
	}
	printf("%d random numbers in [%d, %d]: \n", numCnt, numMin, numMax);
	for (int idx = 0; idx < numCnt; idx++) {
		printf("%d \t", randomNums[idx]);
	}
	return 0;
}
*/
